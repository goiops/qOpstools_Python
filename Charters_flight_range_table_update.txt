#import packages
import pandas as pd
import numpy as np


#import data
file= r'\\corp.qf.qantas.com.au\Profiles\SYDQCX\207634\My Documents\GitHub\hello_world\Charters_data.xlsx'
xls=pd.ExcelFile(file)
print(xls.sheet_names)


#modify data
client_data = xls.parse("Flight ranges",header=None)
routes = xls.parse("Routes",header=0)

#change column names
client_data.columns = ['Client', 'From_', 'To_']

#tidy the data
#remove nulls
client_data.dropna(inplace=True)


#create day of week list and dataframe
dow = ['Mon','Tue','Wed','Thu','Fri','Sat','Sun']
'''dow_df = pd.DataFrame(dow)'''
print(dow_df.head())


#create range of flight numbers

flight_range = []

for row in range(1300,1350):
    flight_range.append(row)
    
print(flight_range)


#create list of routes
routes_list = routes['Departure Port'].unique()
print(routes_list)


#iterate over tables to create flight range table
dow = ['Mon','Tue','Wed','Thu','Fri','Sat','Sun']
#print(flight_range)
routes_list = routes['Departure Port'].unique()

output = []

for row in flight_range:
        
    for day in dow:
        
        for dep in routes_list:
        
            array = np.array([row,day,dep])



#iterate through client table

clients_table = {}

#for i in range(len(client_data.loc[1,:])):
for client, From_, To_ in client_data.iterrows():
    print(client, From,)